const INITIAL_STATE = {
    customerdata: [],
};
export const reducer = (state = INITIAL_STATE, action) => {
    switch (action.type) {
        default:
            return state;
    }
};
//# sourceMappingURL=reducer.js.map